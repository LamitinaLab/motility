# Direct runtime dependencies for Notebooks 1–3.
# Versions are intentionally unpinned for cross-platform installation; archive
# the validated environment separately when exact reconstruction is required.
numpy
pandas
scipy
matplotlib
seaborn
PyYAML
jupyter
ipykernel

# Optional: install plotly to generate interactive 3D PCA HTML.
# Static PCA figures and all numerical analyses run without it.
